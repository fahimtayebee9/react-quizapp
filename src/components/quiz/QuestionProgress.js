const QuestionProgressBar = (() => {
    const display = (props) => {
        return (
            <li>
                <a
                    href=""
                    type="button"
                    className="q-no q-border success"
                    id="cr_"
                >
                    <i className="far fa-circle"></i>
                </a>
            </li>
        );
    };

    return display;
})();

export default QuestionProgressBar;
